import { MatchState } from "../types";
// const gameScore = [Score.Love, Score.Fifteen, Score.Thirty, Score.Forty, Score.Advantage];

// function getPointsScoredDivs(playerScore: Score) {
//   return;
// }
export default function ScoreCircles(_: { matchState: MatchState }) {
  return (
    <div>
      {/* {gameScore.map((_, i) => {
        return <span>{_}</span>;
      })} */}
    </div>
  );
}
